import type { SquadPlayer } from "@/types/squad";

/** Overrides editoriales del jugador; se rellenan desde el CMS. */
export function applyRealAvilesPlayerOverrides(squad: SquadPlayer[]): SquadPlayer[] {
  return squad;
}
